---
title: "Parallax arreglado y layers en niveles del mundo 1"
date: "2026-05-19"
project: "dana-relics-1-dx"
category: "ulpomedia"
tags: ["corrección-bugs"]
---

Esta semana comencé reuniéndome con el diseñador de niveles y el director del proyecto para recibir las tareas pendientes y revisar el estado de avance. Entre las tareas asignadas se encuentran: arreglar el parallax de los fondos (que rompí accidentalmente), integrar los SFX ya listos en el proyecto, añadir un nuevo tipo de plataforma y desarrollar una pequeña mecánica que permita al jugador observar lo que hay bajo sus pies cuando está sobre el suelo o una plataforma.

Durante la jornada de hoy me dediqué principalmente a dos cosas: reparar el sistema de parallax y continuar con la implementación de las capas (sorting layers) para los `SpriteRenderer` en los diferentes niveles.

---

## 1. Parallax Controller

El problema de dirección provenía de cómo se acumulaba el offset de cada capa. En la versión anterior, se sumaba el delta de la cámara directamente (`+= movement`), lo que hacía que las capas se movieran *junto* con la cámara en lugar de hacerlo en sentido contrario. La corrección consistió en negar el movimiento (`-= movement`): cuando la cámara avanza a la derecha (`deltaX > 0`), el offset local de cada capa crece hacia la izquierda. Esto funciona porque el objeto padre ya se ha movido a la derecha junto con la cámara a velocidad completa; al desplazar la capa hacia la izquierda en espacio local, su posición en el mundo se retrasa, generando el efecto parallax correcto. Además, separé la posición base (`baseLocalPositions`) del offset acumulado (`accumulatedOffset`) en arrays distintos, de modo que la base nunca se modifica y el wrapping y el respawn son triviales de calcular.

https://i.imgur.com/BHWPWUh.png

https://i.imgur.com/fe7qhyg.mp4

El otro problema era que el sprite desaparecía porque solo existía una instancia por capa: en cuanto el offset lo sacaba del encuadre, quedaba un hueco. La solución fue generar dos clones en tiempo de ejecución, uno a la izquierda y otro a la derecha, posicionados exactamente a `±width` del tile central. En cada frame, los tres se mueven juntos usando el mismo `accumulatedOffset`. Cuando el tile central hace wrapping (salta un ancho completo), los vecinos saltan con él y la transición resulta invisible. Los clones se crean mediante `SpawnNeighborTiles()`, copiando solo las propiedades visuales del `SpriteRenderer` original (sprite, material, sorting layer, flip) sin duplicar scripts ni componentes de física, utilizando `new GameObject` + `AddComponent` en lugar de `Instantiate`.

https://i.imgur.com/f57lH5I.png

https://i.imgur.com/XHZoqUx.png

https://i.imgur.com/Bc07qBl.mp4

---

## 2. Capas de sprites en el Mundo 1

Para finalizar la jornada, me dediqué a establecer las capas (sorting layers) de los diferentes elementos en algunos niveles del mundo 1. Actualmente están listos los niveles del 1 al 3, incluyendo sus respectivos fondos con el parallax ya funcional.

---

## Tareas completadas

- ~~Arreglar parallax de fondos~~
- Ajustar las capas (SpriteRenderer) para evitar solapamiento en los niveles:
    - ~~Mundo 1: Level_1_1, Level_1_2, Level_1_3~~

## Tareas pendientes

- Aplicar arte y animaciones a todos los enemigos.
- Integrar la música del juego a los niveles y menús.
- Integrar SFX ya listos en el proyecto.
- Ajustar las capas (SpriteRenderer) en los niveles restantes:
    - Mundo 1: Level_1_4 al Level_1_9
    - Mundo 3: Level_3_1
    - Bosses: Boss_Area_0, Boss_Area_1, Boss_Area_2, Boss_Area_3
- Documentar y comentar el resto de scripts del proyecto.
- Limpiar carpetas de prefabs y otros archivos obsoletos.
- Ajustar la dirección del sprite (animación) de las conveyor platforms según su dirección de movimiento.
- Al mantener presionada la tecla S (o abajo) durante un par de segundos estando en el suelo, la cámara debe desplazarse hacia abajo.
- Implementar plataformas que solo se puedan atravesar en una dirección (de abajo hacia arriba o de arriba hacia abajo).
- Cambiar el sprite de las killzones por fluidos (lava, agua, etc.).

---